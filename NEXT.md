# Next question

Do MaleCNS-derived P1 inputs still make HD-on / cVA-off necessary and sufficient?

Parent: [fly_icarus](https://github.com/martialsystems/fly_icarus) `@2cf5fd6`. That SHA is the closed schema finding. Do not restamp it. The 11-cell published-sign slice stays frozen. `--n 1000`, `--unfreeze`, and `--female-brain-icarus` stay stubbed on that tree.

Same frozen battery: 1, 2, 3, 3b, 3c, 3d, copresent, DA1 sweep. Replace the published-sign row with hop-counts / signed synapses onto annotated P1/pC1 from MaleCNS. If `W[P1, DA1]` is shallower than -1.53, 3d can flip and the Icarus costume starts to matter. If it is steeper, `@2cf5fd6` was a preview of the real brake.

n=2. Subject stays one P1 readout. Schema `W_crit = -1.5262` on the 3d pin is the comparison, not the new weight.

This is the first result that can be about the mapped brain.
